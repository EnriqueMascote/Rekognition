name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Release checklist
        run: echo "Validate changelog, tests, SBOM, rollback and approvals"
